apiVersion: v1
kind: ResourceQuota
metadata:
  name: todoapp-quota
  namespace: todoappns
  labels:
    app: todoapp
spec:
  hard:
    requests.cpu: 1
    requests.memory: 1Gi
    limits.cpu: 2
    limits.memory: 2Gi
    pods: "20"

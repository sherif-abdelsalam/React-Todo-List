apiVersion: v1
kind: LimitRange
metadata:
  name: service1-limits
  namespace: service1
  labels:
    app: service1
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 100m
        memory: 128Mi
      default:
        cpu: 250m
        memory: 256Mi
